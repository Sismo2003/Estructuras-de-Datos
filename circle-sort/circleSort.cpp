#include <bits/stdc++.h>
#define enl '\n'
#define ff first
#define ss second
#define sz(n) n.size()
#define fore(i,n) for(int i = 0 ; i != n ; i++)
typedef vector<int> vi;

int main(){
    int tiempo,distancia,resultado;
    cin>>tiempo>>distancia;
    resultado = distancia/tiempo;
    cout<<resultado<<enl;
    return 0;
}